# keymaps

Vial で保存したキーマップ（`.vil`）の置き場。

- Vial の "Save current layout" で書き出した `.vil` ファイルをここに置く。
- キーコードのみで秘匿情報を含まないため、**git 管理対象**（版管理する）。
- 既定のキーマップ自体は [`keyboard.toml`](../keyboard.toml) の `[[layer]]` が持つ。
  ここの `.vil` は Vial での実機編集スナップショット／参考用。
